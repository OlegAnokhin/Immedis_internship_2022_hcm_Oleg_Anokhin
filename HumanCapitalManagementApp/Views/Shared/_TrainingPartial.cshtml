@model AllQualificationTrainingViewModel

<tr>
    <td><p>@Model.Id</p></td>
    <td><p>@Model.Name</p></td>
    <td><p>@Model.From.ToShortDateString()</p></td>
    <td><p>@Model.To.ToShortDateString()</p></td>
    <td><p>@Model.Description</p></td>
    <td>
@*         @if (Model.IsHired)
        {
            <label>✔</label>
            <a asp-controller="Employee" asp-action="Hire" asp-route-id="@Model.Id" method="post" class="btn btn-secondary">Fired</a>
        }
        else
        {
            <label class="text-center">❌</label>
            <a asp-controller="Employee" asp-action="Hire" asp-route-id="@Model.Id" method="post" class="btn btn-secondary">Hired</a>
        } *@
    </td>
    <td><a asp-controller="QualificationTraining" asp-action="Join" asp-route-id="@Model.Id" class="btn btn-secondary">Join</a></td>
    <td><a asp-controller="QualificationTraining" asp-action="Leave" asp-route-id="@Model.Id" class="btn btn-secondary">Join</a></td>
    
    <td><a asp-controller="QualificationTraining" asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-secondary" onclick="return confirm('Are you sure you want to delete this ?')">Delete</a></td>
</tr>